---
import Logo from '/public/LOGOCIIS.svg';
const links : {name:string, href:string}[] = [
    { name: 'PRINCIPAL', href: '/' },
    { name: 'PONENTES', href: '/about' },
    { name: 'CRONOGRAMA', href: '/contact' },
    { name: 'TALLERES', href: '/blog' },
    { name: 'CONCURSOS', href: '/blog' },
    { name: 'INSCRIPCIÓN', href: '/blog' },
];
---

<header class="fixed w-full h-15 flex justify-center items-center bg-gray-800 text-white">
<nav class="w-7xl my-2 flex justify-between items-center">
    <div class="flex flex-wrap">
        <img src={Logo.src} width="150" height="50" alt="logo">
    </div>
    <div class="text-md font-semibold hidden md:flex items-center space-x-4">
        <ul class="flex gap-4">
            {links.map((link) => (
                <li class="inline-block mr-4">
                    <a href={link.href} class="text-white hover:text-gray-400">{link.name}</a>
                </li>
            ))}
        </ul>
    </div>
</nav>
</header>
    